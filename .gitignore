node_modules
.next
out
next-env.d.ts
tsconfig.tsbuildinfo
.DS_Store
.env
.env.local
.env*.local

# Cloudflare / secrets
.wrangler
.dev.vars
.dev.vars.*
.env*
!.env.example

# Regenerated by `npm run types` / `wrangler types` from wrangler.jsonc — see
# that script in package.json, which `npm run build` always runs first.
worker-configuration.d.ts
/guides
